# MongoDB Service
apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
  namespace: student-management
  labels:
    app: mongodb
    tier: database
spec:
  type: ClusterIP
  selector:
    app: mongodb
  ports:
  - port: 27017
    targetPort: 27017
    protocol: TCP
    name: mongodb
